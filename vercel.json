{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "public",
  "routes": [
    { "src": "/api/gov/(.*)", "dest": "/api/gov-proxy?_path=$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
